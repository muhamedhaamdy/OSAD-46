#include "magic_box.h"
#include "screen_utils.h"

void draw_magic_box(int n)
{
  int cell = 80 / n;
  // int i = 1;
  printWithColor("1", "blue", n / 2 * cell, 1);
  // for (; i <= n * n; i++)
  // {

  // }
}